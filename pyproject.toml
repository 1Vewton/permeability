[project]
name = "permeability"
version = "0.2.2"
description = "A python package for calculating permeability"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.0",
    "numpy>=2.4.6",
]

[dependency-groups]
dev = [
    "build>=1.5.0",
    "twine>=6.2.0",
]
test = [
    "flake8>=7.3.0",
    "numpy>=2.4.6",
    "pytest>=9.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
permeability_mcp = "permeability.permeability_mcp.Server:run"
